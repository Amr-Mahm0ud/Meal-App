import 'package:flutter/material.dart';

class YourAccount extends StatelessWidget {
  static const routeName = 'YourAccount';
  const YourAccount({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}